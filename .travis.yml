language: python
python:
  - "3.6"      # current default Python on Travis CI
  - "3.7"
  - "3.8"
  - "3.9"
before_install:
  - sudo apt-get update -qq
  - sudo apt-get install -qq pocl
# command to install dependencies
install:
  - pip install -r requirements.txt
# command to run tests
script:
  - pytest
